export enum Resource {
  USER = 'user',
  ROLE = 'role'
}

export enum RoleName {
  USER = 'user',
  ADMIN = 'admin'
}

export enum Possession {
  ANY = 'any',
  OWN = 'own'
}

export enum Action {
  CREATE = 'create',
  UPDATE = 'update',
  READ = 'read',
  DELETE = 'delete'
}
